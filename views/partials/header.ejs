<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Cluckr</title>
    <link rel="stylesheet" href="/css/bootstrap.min.css" />
    <link rel="stylesheet" href="/css/master.css" />

    <script src="/js/jquery.min.js"></script>
    <script src="/js/popper.min.js"></script>
    <script src="/js/bootstrap.min.js"></script>
  </head>
  <body>
    <header>
      <nav class="navbar navbar-expand-sm bg-light justify-content-between">
        <div>
          <img
            class="chicken-icon"
            src="/images/chicken.png"
            alt="tiny chicken logo"
          />
        </div>
        <div class="nav-buttons">
          <% if(loggedInUserName){ %>
          <div class="username">Welcome back, <%= loggedInUserName %> !</div>
          <form action="/sign_out" method="POST">
            <button class="btn btn-lg btn-secondary" type="button">
              Sign Out
            </button>
          </form>
          <% } else { %>
          <form action="/sign_in" method="POST" class="nav-buttons">
            <button class="btn btn-lg btn-secondary" type="button">
              Sign In
            </button>
          </form>
          <% } %>
          <button class="btn btn-lg btn-secondary" type="button">Clucks</button>
        </div>
      </nav>
    </header>
  </body>
</html>

<!-- I am aware that the closing body tags and html tags should not be here, as they go in the footer. -->
<!-- This seems to be an issue with the 'prettier' extension, a common issue upon googling. It's been like this in all -->
<!-- of my express projects and they still work so I hope that this will not change anything. -->
